<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wisedu.minos.casp.portal.dao.mapper.PageCardConfigMapper">

    <!-- 开启二级缓存 -->
    <cache-ref namespace="com.wisedu.minos.casp.portal.dao.mapper.PageCardConfigMapper"></cache-ref>

    <select id="checkCardByCardWid" resultType="java.lang.String">
        select page.page_code  from
            t_amp_view_page_card_config card
            left join t_amp_view_page_info page
            on card.page_wid = page.wid
            where card.card_wid = #{cardWid}
            and card.is_deleted = '0' and page.is_deleted = '0'
    </select>
</mapper>
